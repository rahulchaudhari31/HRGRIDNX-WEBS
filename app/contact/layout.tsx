import type { Metadata } from "next";
export const metadata: Metadata = { title: "Get started | HRGRIDNX", description: "Talk with the HRGRIDNX team about a better way to run people operations." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
