import type { Metadata } from "next";
export const metadata: Metadata = { title: "Customers | HRGRIDNX", description: "See how growing teams use HRGRIDNX to connect people operations." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
