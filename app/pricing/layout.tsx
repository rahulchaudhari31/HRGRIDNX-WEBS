import type { Metadata } from "next";
export const metadata: Metadata = { title: "Pricing | HRGRIDNX", description: "Simple, transparent plans for connected people operations." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
