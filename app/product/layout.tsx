import type { Metadata } from "next";
export const metadata: Metadata = { title: "Product | HRGRIDNX", description: "Explore the connected HRGRIDNX platform for people, time, pay and progress." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
