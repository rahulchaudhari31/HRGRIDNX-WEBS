import type { Metadata } from "next";
export const metadata: Metadata = { title: "Resources | HRGRIDNX", description: "Practical ideas for people, pay, hiring and better work." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
