import type { Metadata } from "next";
export const metadata: Metadata = { title: "Security | HRGRIDNX", description: "Learn how HRGRIDNX protects people data with thoughtful controls." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
