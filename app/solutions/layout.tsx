import type { Metadata } from "next";
export const metadata: Metadata = { title: "Solutions | HRGRIDNX", description: "Focused people operations for every role in your organisation." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
